apiVersion: v1
kind: Service
metadata:
  name: sentry-postgres
  namespace: sentry
spec:
  selector:
    app: sentry-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP