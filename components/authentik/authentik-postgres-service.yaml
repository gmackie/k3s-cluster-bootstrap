apiVersion: v1
kind: Service
metadata:
  name: authentik-postgres
  namespace: authentik
spec:
  selector:
    app: authentik-postgres
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
  type: ClusterIP